class Datamod {
  String uid;
  String name;
  String age;
  String height;
  String weight;

  Datamod({this.uid,this.name, this.age, this.height, this.weight});

  // Datamod.fromJson(Map<String, dynamic> parsedJSON)
  //     : name = parsedJSON['name'],
  //       age = parsedJSON['age'],
  //       height = parsedJSON['height'],
  //       weight = parsedJSON['weight'];
  // /*factory User.fromMap(Map data) {
  // return User(
  //   name: data['name'] ?? '',
  //   photourl: data['photourl'] ?? '',
  //   totalquestions: data['totalquestions'] ?? '',
  // );*/

}
